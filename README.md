# unique-word-occurences-across-files
Given multiple text files, a C# program will count the occurrences of each unique word in the files and aggregate the results. Processing of the data should be done in a way to minimize running time and memory, independent of the number of files or file size. File size can differ and be large due to many words on one line or many lines in a file.
